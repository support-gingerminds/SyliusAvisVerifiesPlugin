<div class="ui hidden divider"></div>

<h4 class="ui top attached large header">{{ 'ikuzo_avis_verifies.form.title'|trans }}</h4>
<div class="ui attached segment">
    {{ form_errors(form) }}
    <div class="field">
        {{ form_row(form.isAvisVerifiesActive) }}
    </div>

    <div class="two fields">
        <div class="field">
            {{ form_row(form.avisVerifiesSecretKey) }}
        </div>
        <div class="field">
            {{ form_row(form.avisVerifiesWebsiteId) }}
        </div>
    </div>
</div>
